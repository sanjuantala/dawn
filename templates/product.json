/*
 * ------------------------------------------------------------
 * IMPORTANT: The contents of this file are auto-generated.
 *
 * This file may be updated by the Shopify admin theme editor
 * or related systems. Please exercise caution as any changes
 * made to this file may be overwritten.
 * ------------------------------------------------------------
 */
{
  "sections": {
    "main": {
      "type": "main-product",
      "blocks": {
        "vendor": {
          "type": "text",
          "settings": {
            "text": "{{ product.vendor }}",
            "text_style": "uppercase"
          }
        },
        "title": {
          "type": "title",
          "settings": {}
        },
        "subtitle": {
          "type": "text",
          "settings": {
            "text": "{{ product.metafields.descriptors.subtitle.value }}",
            "text_style": "subtitle"
          }
        },
        "price": {
          "type": "price",
          "settings": {}
        },
        "variant_picker": {
          "type": "variant_picker",
          "settings": {
            "picker_type": "button"
          }
        },
        "quantity_selector": {
          "type": "quantity_selector",
          "settings": {}
        },
        "buy_buttons": {
          "type": "buy_buttons",
          "settings": {
            "show_dynamic_checkout": true
          }
        },
        "description": {
          "type": "description",
          "settings": {}
        },
        "share": {
          "type": "share",
          "settings": {
            "share_label": "Share"
          }
        },
        "custom_liquid_3AyeR3": {
          "type": "custom_liquid",
          "settings": {
            "custom_liquid": "{% comment %}\n  Renders bundle composition for a bundled product.\n  Works when the current product's selected variant has \"components\".\n{% endcomment %}\n\n{% assign bundle_variant = product.selected_or_first_available_variant %}\n\n{% if bundle_variant and bundle_variant.components and bundle_variant.components.size > 0 %}\n  <section class=\"bundle-components\">\n    <h3>What’s inside {{ product.title }}</h3>\n    <ul role=\"list\" class=\"bundle-components__list\">\n      {% for comp in bundle_variant.components %}\n        {%- assign child_variant = comp.product_variant | default: comp.variant -%} {# support either key #}\n        <li class=\"bundle-components__item\">\n          <a href=\"{{ child_variant.product.url }}\">\n            {{ child_variant.product.title }}\n          </a>\n          {% if child_variant.title and child_variant.title != 'Default Title' %}\n            — {{ child_variant.title }}\n          {% endif %}\n          × {{ comp.quantity }}\n        </li>\n      {% endfor %}\n    </ul>\n  </section>\n{% endif %}"
          }
        }
      },
      "block_order": [
        "vendor",
        "title",
        "subtitle",
        "price",
        "variant_picker",
        "quantity_selector",
        "buy_buttons",
        "description",
        "share",
        "custom_liquid_3AyeR3"
      ],
      "settings": {
        "enable_sticky_info": true,
        "hide_variants": false,
        "enable_video_looping": false
      }
    },
    "product-recommendations": {
      "type": "product-recommendations",
      "disabled": true,
      "settings": {
        "heading": "You may also like",
        "image_ratio": "adapt",
        "show_secondary_image": false,
        "add_image_padding": false,
        "show_image_outline": true,
        "show_vendor": false,
        "show_rating": false
      }
    },
    "custom_liquid_XngHAg": {
      "type": "custom-liquid",
      "name": "t:sections.custom-liquid.presets.name",
      "settings": {
        "custom_liquid": "123\n\n{% comment %}\n  Renders bundle composition for a bundled product.\n  Works when the current product's selected variant has \"components\".\n{% endcomment %}\n\n{% assign bundle_variant = product.selected_or_first_available_variant %}\n\n{% if bundle_variant and bundle_variant.components and bundle_variant.components.size > 0 %}\n  <section class=\"bundle-components\">\n    <h3>What’s inside {{ product.title }}</h3>\n    <ul role=\"list\" class=\"bundle-components__list\">\n      {% for comp in bundle_variant.components %}\n        {%- assign child_variant = comp.product_variant | default: comp.variant -%} {# support either key #}\n        <li class=\"bundle-components__item\">\n          <a href=\"{{ child_variant.product.url }}\">\n            {{ child_variant.product.title }}\n          </a>\n          {% if child_variant.title and child_variant.title != 'Default Title' %}\n            — {{ child_variant.title }}\n          {% endif %}\n          × {{ comp.quantity }}\n        </li>\n      {% endfor %}\n    </ul>\n  </section>\n{% endif %}"
      }
    }
  },
  "order": [
    "main",
    "product-recommendations",
    "custom_liquid_XngHAg"
  ]
}
