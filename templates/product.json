/*
 * ------------------------------------------------------------
 * IMPORTANT: The contents of this file are auto-generated.
 *
 * This file may be updated by the Shopify admin theme editor
 * or related systems. Please exercise caution as any changes
 * made to this file may be overwritten.
 * ------------------------------------------------------------
 */
{
  "sections": {
    "main": {
      "type": "main-product",
      "blocks": {
        "vendor": {
          "type": "text",
          "settings": {
            "text": "{{ product.vendor }}",
            "text_style": "uppercase"
          }
        },
        "title": {
          "type": "title",
          "settings": {}
        },
        "subtitle": {
          "type": "text",
          "settings": {
            "text": "{{ product.metafields.descriptors.subtitle.value }}",
            "text_style": "subtitle"
          }
        },
        "price": {
          "type": "price",
          "settings": {}
        },
        "variant_picker": {
          "type": "variant_picker",
          "settings": {
            "picker_type": "button"
          }
        },
        "quantity_selector": {
          "type": "quantity_selector",
          "settings": {}
        },
        "buy_buttons": {
          "type": "buy_buttons",
          "settings": {
            "show_dynamic_checkout": true
          }
        },
        "description": {
          "type": "description",
          "settings": {}
        },
        "share": {
          "type": "share",
          "settings": {
            "share_label": "Share"
          }
        },
        "custom_liquid_3AyeR3": {
          "type": "custom_liquid",
          "settings": {
            "custom_liquid": "{% comment %}\n  Example when components are stored in a JSON metafield like:\n  product.metafields.shopify_bundles.components (array of items with variant_id, qty, title/handle)\n{% endcomment %}\n{% assign raw = product.metafields.shopify_bundles.components | default: product.metafields.custom.bundle | json %}\n{% if raw %}\n  {% assign items = raw | parse_json %}\n  {% if items and items.size > 0 %}\n    <section class=\"bundle-components\">\n      <h3>What’s inside {{ product.title }}</h3>\n      <ul role=\"list\" class=\"bundle-components__list\">\n        {% for item in items %}\n          <li class=\"bundle-components__item\">\n            {% if item.handle %}\n              <a href=\"{{ routes.root_url }}products/{{ item.handle }}\">{{ item.title }}</a>\n            {% else %}\n              {{ item.title }}\n            {% endif %}\n            {% if item.variant_title and item.variant_title != 'Default Title' %}\n              — {{ item.variant_title }}\n            {% endif %}\n            × {{ item.quantity | default: 1 }}\n          </li>\n        {% endfor %}\n      </ul>\n    </section>\n  {% endif %}\n{% endif %}"
          }
        }
      },
      "block_order": [
        "vendor",
        "title",
        "subtitle",
        "price",
        "variant_picker",
        "quantity_selector",
        "buy_buttons",
        "description",
        "share",
        "custom_liquid_3AyeR3"
      ],
      "settings": {
        "enable_sticky_info": true,
        "hide_variants": false,
        "enable_video_looping": false
      }
    },
    "product-recommendations": {
      "type": "product-recommendations",
      "disabled": true,
      "settings": {
        "heading": "You may also like",
        "image_ratio": "adapt",
        "show_secondary_image": false,
        "add_image_padding": false,
        "show_image_outline": true,
        "show_vendor": false,
        "show_rating": false
      }
    }
  },
  "order": [
    "main",
    "product-recommendations"
  ]
}
